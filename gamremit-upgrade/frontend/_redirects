# GamRemit — Cloudflare Pages _redirects
# Preserved all original routes + new feature routes

# ── Public payment request page ───────────────────────────────────
/pay/*        /pay.html    200

# ── API routes handled by Cloudflare Functions (functions/api/**) ─
# All /api/* requests are handled automatically by the functions/ directory
# No manual redirect needed — CF Pages auto-routes functions/api/**.js

# ── SPA fallback ─────────────────────────────────────────────────
# (do NOT add /* → /index.html — each page is its own entry point)
